/* 
backend.js 
This file is the js for the backend logic
*/
// this part needs to be able to handle the math for this calculator

// to do this i am thinking of converting the inputBox values into
// numbers instead of strings and doing it that way

// first i need to convert all string to numbers

;
// importing "const inputValue = inputBox.value"
// basically grabbing the variable that holds the inputBox value


// var for button "buttonSum" in index.html file


/*
const operatorString = "+";
let operator;
*/

// function for converting strings to numbers

/*
function convertStringTonum(inputValue) {
  const convertedValue = praserFloat(inputValue);
  return isNaN(convertedValue) ? null : convertedValue;
}
*/

// switch for converting strings to math operators
// then the sum need to be outputted back to the inputbox


